  $(document).ready(function(){	  	$("html").niceScroll();		        $(".mobile-menu-icon.fa-bars").click(function(){        $(".mobile-menu").slideToggle();        });jQuery(document).ready(function($) {		$("#owl-example").owlCarousel({			navigation:true,			navigationText:false,			pagination:false,			autoPlay:	true		});});$(".scroll-down.fa-angle-double-down").click(function(e) {		e.preventDefault();		$('html, body').animate({			scrollTop: $(".offer").offset().top		}, 500);});	// Slideshow 1$("#slider1").responsiveSlides({        auto: false,        pager: true,        nav: true,        speed: 500,        namespace: "centered-btns" });	$(".apply-button").click(function(){        $(".apply-section").slideToggle();        });	$(".angle-wrapper").click(function(){        $(".mobile-button").slideToggle();        });	$(".mobile-service-nav").click(function(){        $(".mobile-serve").slideToggle();        });		$(".sb-search.fa-search").click(function(){        $(".search-wrapper").slideToggle();	});		$(".search-wrapper button").click(function(){        $(".search-wrapper").slideUp();	});			$(".upload-bg").click(function(){        $(".upload-artwork").slideToggle();	});	$(".request-bg").click(function(){        $(".quote-request").slideToggle();	});	$(".upload-bg").click(function(){        $(".quote-request").slideUp();	});	$(".request-bg").click(function(){        $(".upload-artwork").slideUp();	});	$(".upload-bg").click(function() {			    $('html,body').animate({			        scrollTop: $(".upload-artwork").offset().top},			        'slow');			});	$(".request-bg").click(function() {			    $('html,body').animate({			        scrollTop: $(".quote-request").offset().top},			        'slow');			});	$('[placeholder]').focus(function() {		var input = $(this);		if (input.val() == input.attr('placeholder')) {		input.val('');		input.removeClass('placeholder');		}		}).blur(function() {		var input = $(this);		if (input.val() == '' || input.val() == input.attr('placeholder')) {		input.addClass('placeholder');		input.val(input.attr('placeholder'));		}	}).blur();					 });function lightbox(idx) {        //Show the slider wrapper        var ninjaSldr = document.getElementById("ninja-slider");        ninjaSldr.parentNode.style.display = "block";        //Then init the slider        //Note: The { initSliderByCallingInitFunc: true } option in the         // ninja-slider.js tells the page not to initiate the slider        // unless the following init(idx) function is called.        nslider.init(idx);        //Then mimic clicking the fullscreen button to popup the modal        var fsBtn = document.getElementById("fsBtn");        fsBtn.click();    }    function fsIconClick(isFullscreen) {        //Note: fsIconClick is the default event handler of the fullscreen button        if (isFullscreen) {            var ninjaSldr = document.getElementById("ninja-slider");            ninjaSldr.parentNode.style.display = "none";        }    }